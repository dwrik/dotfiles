# a list of all built-in modifier and literal keywords can
# be found at https://github.com/koekeishiya/skhd/issues/1

# open terminal
# shift + alt - return : wezterm

# fuzzy find and open file using default app
shift + alt - f : wezterm start zsh -i -c 'openw $(fzf)'

# simulate arrow keys using caps + hjkl
# needs caps to be mapped to globe in 'System Settings'
fn - h : skhd -k "left"
fn - j : skhd -k "down"
fn - k : skhd -k "up"
fn - l : skhd -k "right"

# navigate word boundaries using option + np
fn - p : skhd -k "alt - left"
fn - n : skhd -k "alt - right"

# window center & fullscreen
# alt - e : skhd -k "fn + ctrl - c"
# alt - f : skhd -k "fn + ctrl - f"

# focus on menu bar
alt - m : skhd -k "fn + ctrl - f2"

# intellij remaps:
# cmd + g : open in github
# cmd + t : open terminal
